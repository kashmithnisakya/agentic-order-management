# OpenAI Configuration (Default)
OPENAI_API_KEY=your_openai_api_key_here
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4000

# Alternative Models (uncomment to use)
# LLM_MODEL=gpt-4o-mini        # Faster, cheaper
# LLM_MODEL=gpt-4-turbo        # More capable
# LLM_MODEL=gpt-3.5-turbo      # Legacy, cheaper

# Alternative: Use Local LLM (Ollama)
# Uncomment these and comment out OPENAI_API_KEY to use Ollama
# LLM_MODEL=ollama/llama3.2
# LLM_BASE_URL=http://localhost:11434
# Make sure Ollama is running: ollama serve

# Server Configuration
HOST=0.0.0.0
PORT=8000
